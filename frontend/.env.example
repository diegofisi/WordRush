# Socket.IO server URL. Leave empty to use the page origin (same-host deploy).
VITE_SOCKET_URL=https://<backend>.up.railway.app

# --- BOSS-MODE (temporary; see docs/context/07-boss-removal.md) -------------
# The optional "challenge the fly" option on the home screen. Default on; set
# it to false and the option, the panel and the brain tab all disappear.
# VITE_BOSS_ENABLED=true
# --- end BOSS-MODE ---------------------------------------------------------
