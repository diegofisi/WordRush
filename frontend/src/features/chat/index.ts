/**
 * Sanctioned facade (see .claude/skills/frontend/references/project.md): the
 * chat is one slice embedded by the game and the results screens.
 */
export { ChatContainer } from './containers/ChatContainer';
export { useChatStore } from './stores/useChatStore';
