/**
 * A teammate's board, letters included: teammates play the same word and see
 * each other live (docs/context/06-v1.1.md -> Teams). The same grid shows
 * every board on the results screen, so it lives in `shared`.
 */
export { LetterBoard as TeammateBoard } from '@/shared/components/ui/LetterBoard';
